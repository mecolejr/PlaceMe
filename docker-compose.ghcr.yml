version: '3.9'
services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      - POSTGRES_USER=trueplace
      - POSTGRES_PASSWORD=trueplace
      - POSTGRES_DB=trueplace
    volumes:
      - db_data:/var/lib/postgresql/data
  backend:
    image: ghcr.io/YOUR_ORG/trueplace-backend:TAG
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql://trueplace:trueplace@db:5432/trueplace?schema=public
      - NODE_ENV=production
    ports:
      - '4000:4000'
  frontend:
    image: ghcr.io/YOUR_ORG/trueplace-frontend:TAG
    depends_on:
      - backend
    ports:
      - '5178:80'
volumes:
  db_data:
